# README

Fm is a simple way to rename bulk files in Vim. It requires Vim by
default, but you can use any editor by changing the constant `EDITOR` in the
code.

### Install

```sh
go build -o ~/.local/bin/fm main.go
```
### Run

```sh
fm <path_to_files_you_want_to_rename>
```
